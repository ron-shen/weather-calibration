# Data

This directory is for generated synthetic CSV files.

Generate a dataset with:

```bash
python -m weather_calibration make-data --output data/synthetic_weather.csv
```

Generated CSVs are ignored by Git.
